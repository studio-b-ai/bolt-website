{
  "name": "bolt-website",
  "version": "1.0.0",
  "private": true,
  "description": "Bolt by Studio B — product marketing site for bolt.b.studio",
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.5"
  },
  "engines": {
    "node": ">=18"
  }
}
